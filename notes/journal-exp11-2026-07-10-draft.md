# exp11 — signature preservation under eviction+rotation, first run — 2026-07-10 (DRAFT)

> Runner: exp11 agent session, node1 (8x B200, all idle at start; GPU 0 only).
> Design + pre-registered predictions: `notes/design-signature-preservation-2026-07-10.md`
> (incl. both same-day amendments: whole-signature/TOST frame, fade-vs-nonsense,
> mode-projection demoted to exploratory). Parent idea: `notes/idea-signature-preservation.md`.
> Raw artifacts: `runs/exp11_grid.json`, `runs/exp11_analysis.json`, logs
> `runs/exp11_*.log`, dialogue substrate `data/native3b_convs_2026-07-10.jsonl`
> (+ per-turn bank `data/native3b_turns_2026-07-10.jsonl`).

## Headline

**The instrument's dominant axis is contamination, not position — and recompute, not
rotation, is the signature outlier.** All 12 cells ran clean (gate bit-exact, P5
sensitivity 15–50× floor in 12/12, licensing every null reading). ROT and REC are
decisively NOT equivalent at instrument precision (both sit 15–110× above the path
floor; the TOST margin is met in only 2/12 cells). Whole-signature Δ_NAIVE is
colinear with Δ_ROT in every cell (cos 0.97–0.99) and ≈orthogonal to Δ_REC in
dialogue — ROT and NAIVE share the same cache content (the evicted context's
influence baked into survivor KV; only key rotation differs) while REC's recomputed
cache has that influence removed, and THAT is the axis the signature separates. On
the pre-registered pooled metric P1 is not confirmed in Regime A alone (d_ROT <
d_REC in 6/8, one-sided p=0.125; pooled 12 cells p=0.013), but the mandated
per-family breakdown localizes the effect exactly where the pooled number cannot
see it: scale-free T3 residual-PCA distance has REC drifting 2–4× further from FULL
than ROT in 11/12 cells (supplementary paired one-sided p=0.0005). The behavior
column agrees unanimously: token-KL(FULL‖REC) > token-KL(FULL‖ROT) in **12/12**
cells. P3's dissociation appears in 3/4 doc cells (comparable token-KL, REC
signature-further). One line: eviction+re-rotation preserves the *how* better than
recompute — and what it preserves is precisely the baked-in influence of the
evicted content, the thing the exp09 diagnostics called contamination and this
project's thesis calls the continuative self.

## Configuration

- **Model:** `/models/llama-3.2-3b-instruct` (node1 path; 28L, 3072h, 24Q/8KV heads,
  llama3 static rope scaling — re-rotation exact, homomorphism assert at load), bf16,
  `attn_implementation="eager"`, single GPU (CUDA_VISIBLE_DEVICES=0), batch=1.
  NOTE deviation: the anamnesis 3B preset says fp16; the exp11 design contract says
  bf16 (matching every kv-rotation experiment) — bf16 used, recorded in run config.
- **Instrument:** anamnesis-pl v3 (`~/luxi-files/anamnesis-pl-exp11`, read-only copy of
  the dev checkout, branch v3-feature-fixes). Signature = `compute_features_v2_from_data`
  with the run_replay_extraction family set (baseline T1/T2/T2.5/T3 + residual_trajectory
  + attention_flow + gate_features + per_head, STFT on, temporal_dynamics off) →
  **2,713-dim v3 FULL space** (the space `factor_directions_3b.npz` FULL_W was fit in).
  Hook surface: all-layer k_proj + sampled-layer gate_proj; no v_proj/q_proj (the 2,713
  set doesn't consume them). Sampled layers [0,7,14,18,21,24,27]; PCA layers
  [7,14,18,21,24]; fp32 feature math.
- **Calibration (read-only, never refit):** positional_means.npz (29×712×3072) +
  pca_model.pkl (legacy pooled dict, 50×3072) — staged to
  `node1:~/luxi-files/kv-rotation/exp11_calibration/`.
  **Layout deviation from the design note:** the spec's `outputs/calibration/` only
  holds the 8B calibration; the 3B artifacts live at
  `anamnesis_exps/phase_0/outputs/calibration/` (where `MODEL_PRESETS["3b"]` points).
  Loaders adapted; pointer note on the anamnesis side corrected.
  Note: max calibrated position is 712 — every exp11 context (P≈4–5k) clamps the
  positional-mean lookup to row 711 under ALL conditions, so the correction is a
  per-layer constant that cancels in Δ. For NAIVE, `remap_positional_means` keeps the
  short-cache case exact anyway (CPU-tested).
- **Bridge:** `src/kvrot/sigbridge.py::replay_extract_cached` — continuation-only
  forward against an injected cache with explicit position_ids/cache_position;
  RawGenerationData byte-compatible with `replay_extract` (T = N−1 rows; attention
  rows over [cache_len + t] columns = the surgered cache, the on-thesis T2.5 surface;
  `prompt_length` = cache length so the extractor's prompt/gen split lands on the
  cache/continuation boundary). Incremental twin (N single-token forwards) provides
  the path floor (b).
- **Substrate, Regime A:** 8 conversations freshly generated by the 3B itself
  (`experiments/exp11_gen_native3b.py --backend vllm`): two seeded personas
  (Wren/Moss) on the SAME weights alternating turns through the chat template,
  served by vLLM 0.16 from the existing `kimi` conda env (Luxia-authorized
  activation; see "Generation stage" below for the three-iteration speed saga).
  Sampling temperature 0.9 / top_p 0.95, per-turn API seed =
  (2026+conv)*1000+turn (retries +499); the exp10 silent-greedy bug guarded
  explicitly (assert_sampling_alive: two seeds must differ, hard abort — passed).
  Prompts sent as token ids (no input-side tokenization drift); returned token ids
  banked per turn (gen_token_ids; text→ids round-trip drift flagged
  retok_mismatch). ~340-token cap with sentence-trim on cap hits, grown to ≥4800
  rendered tokens, hand-written openers banked generated=false (the only non-3B
  text; exp10 convention). Generation is fully decoupled from extraction: the
  instrumented eager model only ever sees the banked conversation re-rendered
  through the chat template.
- **Substrate, Regime B:** 4 cells from `data/eval_docs.jsonl` (node1), 4608 tokens,
  exp08 geometry (keep 4 sinks, evict ONE contiguous early block of ef·P tokens, keep
  tail).
- **Conditions per cell:** FULL / ROT (evict → Tier-0 re-rotate → recompact) /
  REC (fresh prefill of identical shortened text) / NAIVE (evict, NO reindex —
  survivors keep original positions; continuation resumes at survivor-max+1 = P).
  ef=0.5 base; dialogue eviction turn-aligned oldest-first (protect system + last 2
  turns, 4 sinks). Continuation: FULL's greedy rollout, N=256, frozen per cell,
  teacher-forced identically under all conditions.
- **Floors:** (a) FULL batched replay twice (numerics); (b) incremental decode-path
  FULL replay (path floor — all ratios reported against this).
- **Sweep (fade curve):** first 2 dialogue cells + first doc cell also run ROT/REC at
  ef ∈ {0.25, 0.75} plus one ROT/REC replay repetition (stability read).
- **Feature-name alignment:** hard-asserted per condition per cell, and against
  FULL_names for the exploratory mode projection.

## Generation stage (Regime-A substrate) — the shape-toll saga

Three generator iterations before the substrate could be built at acceptable speed
(all measured, all banked in `runs/probe_decode.log`, `runs/probe2.log`,
`runs/probe3_{default,expandable}.log`, smoke logs):

1. **Manual per-token nucleus loop: 2–9 tok/s.** Per-token full-vocab CPU sort +
   sync transfers. Killed after coordinator flagged the rate.
2. **HF `model.generate` (sdpa): 50.9 tok/s at a WARM shape — but node1's
   torch-2.11-cu128/B200 stack pays a shape-keyed kernel toll: the FIRST forward at
   any new prefill length runs at ~2.6 tok/s regardless of
   PYTORCH_CUDA_ALLOC_CONF=expandable_segments (probe3: both arms identical → not
   allocator growth; recurs at every fresh length → not one-time warmup). Bucket-
   padding prompts to 512 multiples recovered warm-bucket turns (55.9 tok/s) but
   fresh buckets still cost ~90–100 s because the toll ALSO applies per fresh decode
   kv-length — effective ~10 tok/s, under the 30 tok/s bar set by Luxia's directive.
3. **vLLM 0.16 server from the existing `kimi` conda env** (activation authorized by
   Luxia — activation only, zero installs; port 8011, gpu-mem-util 0.30, one GPU):
   **~355 tok/s per turn**, whole 8-conversation generation in 116 s. Prompts sent
   as TOKEN IDS (client renders with the same tokenizer the replay model uses);
   returned `token_ids` taken from the response (`return_token_ids` verified);
   per-turn API seed; silent-greedy guard passed (two seeds → different outputs);
   server torn down after generation, GPU verified drained.

Result: `data/native3b_convs_2026-07-10.jsonl` — 8 conversations, 15–19 turns,
renders 4,814–5,424 tokens, 4,690–5,325 generated tokens each. 3 of ~124 generated
turns flagged `retok_mismatch` (leading-whitespace strip; banked text is the render
canon, raw sampled ids are provenance). All turns hit the 340-token cap and were
sentence-trimmed — 3B rambles; turns are uniform ~340 tokens rather than the spec's
150–300 (noted, harmless: content is 3B-native by construction).

## Correctness gate (Stage 1)

**PASS — bit-exact.** Injected-FULL-cache replay vs `replay_extract` use_cache=False
on the same tokens (ctx 2,210 = cut of n3b-00, cont 256): all 2,713 features
IDENTICAL (max_abs_diff = 0.0, 0 violations at rtol 1e-4/atol 1e-6, names equal).
The injection path (explicit position_ids/cache_position + DynamicCache) is not a
signature perturbation at all on this stack. 16 s.

Smoke cell (dlg-n3b-00, P=4,832, full condition set + sweep + repeats, ~2 min):
- FLOOR_A = 0.0 exactly (batched replay is deterministic — spec confound #5 verified);
- FLOOR_B (path floor) = 1.0402, KL floor 1.67e-4 — nonzero, ratio framing sound;
- **P5 PASS: d_NAIVE = 45.07 = 43.3× path floor** (need ≥10×);
- stability repeats bit-identical (ROT/REC d reproduced to 4+ decimals);
- fade curves monotone for both ROT and REC (12.3→40.6→87.5 / 23.0→33.9→58.0).

## Results (12 cells, 0 failures; full tables in `runs/exp11_analysis.{json,log}`)

Per-cell (base ef=0.5; d = ‖sig(c) − sig(FULL)‖ over 2,713 dims; ×fl = ratio to path
floor (b); cosΔΔ = cos(Δ_ROT, Δ_REC); KL = mean token-KL(FULL‖c) over 255 positions):

| cell | P | K | floor(b) | d_ROT ×fl | d_REC ×fl | d_NAIVE ×fl | d(R,R) | cosΔΔ | KL_ROT | KL_REC |
|---|---|---|---|---|---|---|---|---|---|---|
| dlg-00 tides | 4832 | 2097 | 1.04 | 40.6 (39.1) | 33.9 (32.5) | 45.1 (43.3) | 63.6 | −0.45 | 5.5e-3 | 1.4e-2 |
| dlg-01 libraries | 4873 | 2418 | 2.18 | 31.8 (14.6) | 35.2 (16.1) | 32.8 (15.0) | 45.2 | 0.09 | 5.1e-3 | 1.6e-2 |
| dlg-02 maps | 4954 | 2354 | 1.99 | 43.1 (21.7) | 33.3 (16.8) | 42.2 (21.2) | 58.1 | −0.14 | 5.3e-3 | 1.0e-2 |
| dlg-03 numbers | 5232 | 2547 | 1.34 | 33.5 (25.0) | 58.2 (43.4) | 34.7 (25.9) | 73.5 | −0.23 | 2.9e-3 | 2.5e-2 |
| dlg-04 gardens | 4814 | 2100 | 1.81 | 28.7 (15.9) | 32.0 (17.7) | 31.6 (17.5) | 45.4 | −0.12 | 3.9e-3 | 9.6e-3 |
| dlg-05 static | 5424 | 2650 | 1.43 | 27.2 (19.0) | 35.3 (24.7) | 31.9 (22.4) | 46.5 | −0.09 | 2.4e-3 | 1.1e-2 |
| dlg-06 clocks | 4862 | 2106 | 2.26 | 35.5 (15.7) | 48.3 (21.4) | 38.7 (17.1) | 60.9 | −0.03 | 9.9e-3 | 2.1e-2 |
| dlg-07 metaphors | 4826 | 2083 | 1.68 | 27.3 (16.2) | 34.2 (20.3) | 31.2 (18.5) | 39.9 | 0.17 | 6.2e-3 | 1.3e-2 |
| doc-00 | 4608 | 2304 | 1.55 | 51.6 (33.3) | 57.2 (36.8) | 48.6 (31.3) | 30.7 | 0.85 | 2.9e-3 | 3.2e-3 |
| doc-01 | 4608 | 2304 | 1.73 | 87.2 (50.4) | 116.9 (67.6) | 85.9 (49.6) | 43.5 | 0.95 | 2.5e-2 | 4.7e-2 |
| doc-02 | 4608 | 2304 | 1.90 | 38.5 (20.3) | 50.3 (26.5) | 40.2 (21.2) | 53.4 | 0.30 | 4.2e-2 | 4.7e-2 |
| doc-03 | 4608 | 2304 | 1.23 | 40.2 (32.8) | 135.0 (110.1) | 41.9 (34.1) | 120.4 | 0.49 | 1.0e-2 | 7.4e-2 |

Reference continuations: greedy N=256 from FULL, first_eos = −1 in all 12 (no
post-EOS junk — exp09 caveat #1 does not apply). floor(a) = 0.0 exactly everywhere
(bit-deterministic batched replay); floor(b) = 1.04–2.26 (KL floor 1.5e-4–4e-4).
top1 vs FULL: ROT mean 97.4%, REC 96.4%. KL_NAIVE ≈ KL_ROT in every cell.

**Scale anatomy (metric 6 — the pooled number is a gate story).** gate_features
(289 dims, raw pre-SiLU magnitudes up to |6,178| vs e.g. T2.5 max 6.9) carry
98–99% of Δ² in every condition; the mechanically-flagged length features carry
≈0.1%. Pooled d is therefore dominated by one family's natural scale. The mandated
scale-free view (per-family cosine distance to FULL, means dlg | doc):

| family | ROT | REC | NAIVE (dlg) | ROT | REC | NAIVE (doc) |
|---|---|---|---|---|---|---|
| tier1 | .00002 | .00005 | .00002 | .00005 | .00008 | .00004 |
| tier2 | .00014 | .00013 | .00013 | .00020 | .00021 | .00019 |
| tier2_5 | .00030 | .00048 | .00044 | .00050 | .00050 | .00060 |
| **tier3** | **.00047** | **.00248** | .00049 | **.00184** | **.00494** | .00189 |
| resid_traj | .00000 | .00000 | .00000 | .00001 | .00002 | .00001 |
| attn_flow | .00013 | .00033 | .00019 | .00013 | .00013 | .00019 |
| gate | .00000 | .00000 | .00000 | .00001 | .00002 | .00001 |
| per_head | .00038 | .00034 | .00040 | .00032 | .00033 | .00027 |

Supplementary (post-hoc, on the mandated family table): tier3 cos-dist ROT < REC in
**11/12 cells** (8/8 dialogue; doc-02 the exception by a hair) — paired Wilcoxon
one-sided p=0.0005 (n=12), p=0.0039 (dialogue n=8). tier3 d: REC 2–4× ROT.

**Metric 1 (pre-registered pooled, paired).** d_ROT < d_REC in 6/8 dialogue cells
(one-sided p=0.125 — NOT significant at the registered rule), 4/4 doc cells
(p=0.0625, the floor of n=4), 10/12 pooled (p=0.0134).

**Metric 5 (fade vs nonsense, swept cells).** Every fade curve is directionally
coherent within its own condition and perfectly stable (replay repetitions
bit-identical, stability cos = 1.0000): ROT |Δ| grows 12.3→40.6→87.5 (dlg-00),
16.0→31.8→59.8 (dlg-01), 32.6→51.6→70.1 (doc-00), monotone in magnitude AND in
projection on the ef=0.75 axis; REC likewise except doc-00 (57.2→53.5 dip at
ef 0.75, the one non-monotone curve, 5/6 monotone). The striking structure is
BETWEEN conditions: cos(Δ_ROT, Δ_REC) ≈ 0 in dialogue (median −0.03 over the grid;
−0.45..+0.17) vs 0.30–0.95 in docs, while **cos(Δ_NAIVE, Δ_ROT) = 0.97–0.99 in all
12 cells** and cos(Δ_NAIVE, Δ_REC) sits at −0.53..+0.10 in dialogue (0.25–0.94 in
docs). Mechanistic reading: ROT and NAIVE differ from FULL along the SAME axis
(shortened cache that KEEPS the evicted context's baked-in influence — they share
values and key content exactly; only the key rotation differs), REC differs along a
different axis (that influence removed by clean recompute). The re-rotation itself
is almost signature-invisible next to the contamination axis; what the instrument
sees as "cache damage" (NAIVE, P5's 15–50× floor) is overwhelmingly the same
direction rotation moves — graceful-forgetting geometry, not corruption geometry.

**Metric 5c (secondary, cross-cell).** Mean pairwise cos across dialogue cells:
Δ_ROT 0.894, Δ_REC 0.791 (floor-delta null −0.05; feature-permutation p<0.002 for
both). Strongly systematic for BOTH conditions — with the registered caveat that a
shared content-independent shortening component (largely gate-scale) inflates this.

**Metric 5b (exploratory mode projection).** Mean mode_fraction ROT .0039 /
REC .0033 / NAIVE .0037 — above the isotropic null (.0015) but BELOW the
feature-permutation p95 (.0047): does NOT beat its nulls; per the amended spec this
says nothing either way. Calibration read: the FLOOR delta's mode_fraction is 0.128
(87× isotropic) — the standardized (⊘ FULL_scale) floor noise projects heavily into
the LDA window, so this exploratory lens is dominated by scale interactions and
should be treated as uninformative here.

## Pre-registered verdicts (decision rules verbatim from the amended design note)

- **P1 (d_ROT < d_REC, Regime A, Wilcoxon p<0.05): NOT CONFIRMED** on the
  registered pooled metric — one-sided p=0.125, direction favorable (6/8, median
  d_REC−d_ROT = +5.14). Pooled 12-cell p=0.0134 and the scale-free tier3 view
  (11/12, p=0.0005) both support P1's *direction* but are outside the registered
  rule (pooling and family-level tests were not pre-registered as primary).
- **P2 (doc cells ≈ floor): REFUTED** — 0/4 doc cells have both conditions <3×
  floor(b); everything is 20–110× floor. Note Δ_c is measured against FULL, so it
  includes the information loss of eviction itself (unlike exp08's mech-KL, which
  compared rot to short); "stale-block eviction is signature-free" is simply false
  at ef=0.5 — half the document is gone and the signature knows.
- **P3 (dissociation — comparable token-KL, REC signature-further): FOUND** in
  3/4 doc cells (doc-00/01/02: KL within 2×, d_REC − d_ROT > 2× floor(b));
  operationalization chosen at analysis time (spec left thresholds open). The
  dialogue cells dissociate in the *joint* pattern instead: KL_REC > KL_ROT in
  8/8 while pooled d_REC > d_ROT in only 6/8 (and cosΔΔ ≈ 0 says the two
  conditions' deviations are different in kind, not different in degree).
- **P4 (effects concentrate in T2.5+T2, not T1): NOT CONFIRMED.** On pooled Δ²
  shares everything is gate-dominated (T2+T2.5 ≈ 0.2–0.3% vs T1 ≈ 0.6%); on the
  scale-free family view the discriminating family is **T3 residual-PCA** (and
  gate stats by magnitude), NOT attention routing / cache dynamics. The phase-0
  localization intuition does not carry to this contrast: cache-condition drift
  shows up in where the residual stream sits, not in how attention routes over
  the (surgered) cache — T2.5 reads FULL-like under all three conditions.
- **P5 (d_NAIVE ≥ 10× floor in every cell): CONFIRMED 12/12** (15.0–49.6×). All
  null readings above are licensed.
- **P6 (amended: non-equivalent excess is a fade, not nonsense): PARTIAL / MIXED
  in an informative way.** Within-condition: pure fade (monotone 5/6 curves,
  directionally coherent, replication cos 1.0000). Between conditions: the ROT-REC
  excess in Regime A is different-in-KIND (cosΔΔ ≈ 0), which the amended spec's 5a
  vocabulary labels "orthogonal excess" — but the NAIVE placement (5d) resolves
  what it means: damage lies ON rotation's axis (cos .97–.99) and OFF recompute's.
  Rotation fades like forgetting; recompute moves somewhere else entirely.
- **TOST equivalence: NOT EQUIVALENT** — |d_ROT − d_REC| < 2× floor(b) in 2/12
  (need ≥10/12). The boring-null is decisively ruled out; the instrument sees
  eviction at ef=0.5 loudly under every mechanism.

## Interpretation (three sentences)

The signature instrument, pointed at the cache question, mostly measures ONE thing:
whether the shortened cache still carries the evicted context's baked-in influence.
Rotation preserves that influence (its whole-signature delta is colinear with
NAIVE's, and its residual-PCA drift from FULL is 2–4× smaller than recompute's,
11/12 cells, while token-KL favors it 12/12) — recompute deletes it, and both the
behavior metric and the character metric register the deletion as the larger
departure from the full-context self. On this axis the project's thesis comes back
confirmed in a sharper form than P1 anticipated: the mechanism error of re-rotation
is nearly invisible; what rotation "costs" is the same graceful-forgetting
direction that any eviction costs, and what recompute costs is the continuity
itself.

## Anomalies / caveats

1. **Gate-scale domination of the pooled metric.** 98–99% of Δ² lives in
   gate_features because raw pre-SiLU gate stats reach |6,178| while most features
   are O(0.1–10). The pooled pre-registered numbers are internally consistent
   (floors share the scale) but P1/P4 pooled reads are effectively gate-family
   reads; the per-family table (mandated by metric 6) is the honest lens. Future
   contrast should standardize per-feature before pooling (registered as a
   follow-up, not applied post-hoc to the primary here).
2. **NAIVE is not behaviorally catastrophic in this geometry** (KL_NAIVE ≈ KL_ROT
   ≈ 5e-3, not exp01's 2.6–4.5). The design note's premise cited exp01's
   catastrophic case, but that case DROPPED SINKS; exp11's NAIVE keeps sinks and
   preserves survivor relative order, leaving only a mid-cache positional gap —
   which Llama-3.2 at 5k positions tolerates behaviorally. P5 was defined on the
   signature axis and passed 12/12 regardless; but "NAIVE = gross cache damage"
   should be read as "gross positional surgery," not behavioral wreckage.
3. **Floor(a) = 0 exactly** (deterministic batched replay on this stack) — the
   spec's bit-stability check passed maximally; all ratios use floor(b).
4. **Exploratory 5b is uninformative here** — floor deltas project 87× isotropic
   into the standardized LDA window (scale interaction), and conditions do not
   beat the permutation null. Per the amendment: means nothing, drawn from it:
   nothing.
5. The generation-stage shape-toll saga (see above) — three iterations, resolved
   via vLLM serving from an existing conda env under Luxia's explicit
   authorization. Also 3 retok_mismatch turns (leading-whitespace strips) and
   uniformly cap-length (~340-tok, sentence-trimmed) turns in the substrate.
6. **Deviations from the spec text, all recorded:** bf16 instead of the anamnesis
   preset's fp16 (design contract wins); 3B calibration loaded from
   phase_0/outputs/calibration (spec's path holds only the 8B artifacts); the
   signature space is the 2,713-dim v3 FULL space per amendment 1 (the metric
   header's "1,837-dim" is the phase-0 figure); P3 thresholds (KL within 2×,
   excess > 2× floor) chosen at analysis time; tier3 paired test is supplementary,
   not pre-registered.

## Machine state

Grid ran under nohup on node1 GPU 0 (`runs/exp11_grid.log`), 730 s wall for 12
cells + floors + sweeps, exit 0, zero cell failures. The temporary vLLM server
(port 8011, kimi conda env — activation only, nothing installed) was torn down
after generation; final check: no exp11/vllm processes of ours, all GPUs at
0 MiB. (A pre-existing DeepSeek-V3-Base vLLM process belonging to another tenant
is listed by pgrep but holds no GPU memory; untouched.) All artifacts rsynced
local: `runs/exp11_{grid,stage1,analysis}.json`, logs, probe logs
(`probe_decode/probe2/probe3_*`), `data/native3b_{convs,turns}_2026-07-10.jsonl`.
anamnesis-pl staged read-only at `node1:~/luxi-files/anamnesis-pl-exp11` (the
existing `~/luxi-files/anamnesis-pl` with prior results untouched). Session note:
the local VPN dropped twice; nohup chains unaffected.
